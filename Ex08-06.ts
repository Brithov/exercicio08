console.log(" ");

    6º Questão()
    Lance um erro no construtor e nos métodos sacar e depositar para que, caso o
    valor passado seja menor que zero uma exceção seja lançada. Reexecute os
    testes da questão anterior com valores que “passem” pelo saldo insuficiente, e
    teste também a chamada dos métodos passando como parâmetro valores < 0.
    
    Ao implementar o exceção no construtor para saldo menor que zero e executar o código 
    temos uma interrupção do código uma vez que o saldo menor que zero impossibilita a criação
    da instancia conta.
    Da forma análoga acontece com os métodos Transferir e depositar, ambos são interrompidos ao 
    ser inserido valores onde a exceção é lançada.
